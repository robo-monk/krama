fn to_i32(any n) {
    return @cast(n, i32);
}

fn i64 fac(i64 a) {
    if a <= 1 {
        return 1
    }

    return a * fac(a-1)
}

fn main() {
    let a = fac(5);
    printf("result~%ld", a);
    return 0:to_i32(5)
}
