{
  let b = {
    let isTrue = !(5 >= 4 + 2 * 3);
    isTrue;
    let a = {
      let hello = 2
    let a = {
        let hello = 2
      let a = {
          let hello = 2
        };
      };
    };
  };

  b + b
  b
}
