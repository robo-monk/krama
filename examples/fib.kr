extern fn void printf(any n0, any n1);

fn u64 factorial(u64 n) {
    if n < 1 {
        return 1
    }
    return factorial(n - 1) * n
}


fn i32 main() {
    let result = factorial(5)
    let b = result + result;
    result;
    printf(result, result, "%s", result)

    return 1
}
